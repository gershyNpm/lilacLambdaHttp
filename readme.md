# Lilac Lambda Http

Lambda specific to http requests.

## Tech debt

This module is specific to aws. Should consider supporting non-aws http lambdas, which will probably mean this repo requires a rename, but this is a longer-term effort.